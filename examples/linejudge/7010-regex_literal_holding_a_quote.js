// Reconstruction of LineJudge case 7010, written from the case
// description in spec 07 01-conformance.md rather than copied from the suite,
// which is not checked out here. It reproduces the counts the spec records.
const re = /["']/;
// a comment
const x = 1;
